<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.com.misa.ccl">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MISACCL">
        <activity android:name=".View.ShopSetup.ActivityAppInformation"></activity>
        <activity android:name=".View.login.ActivityLogin" />
        <activity android:name=".View.login.ActivityAccountRegister" />
        <activity android:name=".View.login.ActivitySelectionOptionLogin" />
        <activity android:name=".View.ShopSetup.ActivityShopSetup" />
        <activity android:name=".View.Welcome.ActivityAppConfirm" />
        <activity
            android:name=".View.Welcome.SplashActivity"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>
    </application>

</manifest>